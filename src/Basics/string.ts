(() => {

    const productTitle: string = "computer"
    const productPrice: number = 1000;
    const available: boolean = true;

    const product1: string = `
    Product: ${productTitle}
    Price: ${productPrice}
    available: ${available}
    `
})()